# setup-matt-pocock-skills 生成的文档 — 仅本地保留，不上传云端
# (docs/agents/ 的 issue-tracker / triage-labels / domain 等)
docs/

# 构建产物
desktop/build/
build/

# java 构建
server/target/

# android
mobile/.gradle/
mobile/build/
mobile/app/build/
mobile/local.properties

# 本地数据库/测试临时文件
*.db

# 工具产物
.reasonix/